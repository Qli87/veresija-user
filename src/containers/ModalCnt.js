import { connect } from 'react-redux'
import ModalCmp from '../components/ModalCmp';

const mapStateToProps = state => ({

})

const mapDispatchToProps = dispatch => ({

})

const ModalCnt = connect (
    mapStateToProps,
    mapDispatchToProps
)(ModalCmp)

export default ModalCnt